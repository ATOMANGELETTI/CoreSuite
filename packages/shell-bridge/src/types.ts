/** Mirrors `coresuite_core::PingRequest`. */
export type PingRequest = {
  message: string;
};

/** Mirrors `coresuite_core::PingResponse`. */
export type PingResponse = {
  message: string;
};

/** Mirrors `coresuite_core::SuiteInfo`. */
export type SuiteInfo = {
  name: string;
  version: string;
};
