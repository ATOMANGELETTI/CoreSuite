# packages

Shared TS/Svelte only.

- `shell-ui` — chrome/UI. Manifest: `shell-ui/src/package.json`. Nord + Svelte 5.
- `shell-bridge` — IPC client
- `tsconfig` — shared compiler defaults

pnpm-only. Shared versions go in the repo-root `catalog:`. No `npm install`. See `.cursor/context/stack.md`.
